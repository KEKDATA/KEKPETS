import { breeds } from './breeds';
import { colors } from './colors';
import { tails } from './tails';
import { types } from './types';

export const searchSettingsOptionsMocks = {
  breeds,
  colors,
  tails,
  types,
};
